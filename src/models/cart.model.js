// Thành viên 3 - Khách mua hàng
// Model = nơi DUY NHẤT chứa câu lệnh SQL của nhóm chức năng này (controller không viết SQL).
// Bảng / đối tượng DB liên quan: Carts, CartItems
// Hàm gợi ý: getOrCreateCart(customerId), items(customerId), add(customerId, productId, qty), update(), remove(), count(customerId)
const db = require('../config/db');

// exports.example = () => db.query('SELECT ...', { param: value });

module.exports = {};
