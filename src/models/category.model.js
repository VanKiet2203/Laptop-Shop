// Thành viên 2 - Danh mục sản phẩm
// Model = nơi DUY NHẤT chứa câu lệnh SQL của nhóm chức năng này (controller không viết SQL).
// Bảng / đối tượng DB liên quan: Categories
// Hàm gợi ý: tree(), findBySlugWithChildren(slug), create(), update(), remove()
const db = require('../config/db');

// exports.example = () => db.query('SELECT ...', { param: value });

module.exports = {};
